name: Dispatch test

on:
  repository_dispatch:
    types: [story-ready-for-ai]

jobs:
  echo-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Print payload
        run: |
          echo "Issue key: ${{ github.event.client_payload.issue_key }}"
          echo "Summary: ${{ github.event.client_payload.summary }}"
          echo "URL: ${{ github.event.client_payload.url }}"
